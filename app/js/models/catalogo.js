define([],function () {
	        return Backbone.Model.extend({
	            defaults: {
	            },
	            urlRoot: 'api/index/catalogo',
	            initialize: function () {
	            }
	        });
		});